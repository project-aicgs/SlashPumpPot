[build]
  publish = "."

# Proxy API calls from the static site to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://YOUR-RENDER-SERVICE.onrender.com/:splat"
  status = 200
  force = true


